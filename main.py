from tkinter import *
from google_currency import *
ops = list(CODES.keys())
window = Tk()
product = Label(window, text='You have not done any conversions yet.')
product.grid(row=0, column=0)
fromThing = None # should finish!!!

